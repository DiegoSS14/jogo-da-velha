spring:
    application:
        name: back_end
    security:
        oauth2:
            resourceserver:
                jwt:
                    issuer-uri: http://127.0.0.1:8081

# spring.security.user.name=admin
# spring.security.user.password=admin